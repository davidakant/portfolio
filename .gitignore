# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dist-standalone
*.local

# Full-resolution source media (kept locally, not shipped or version-controlled —
# see README "Adding real project media")
originals/

# CaletaResidences/Isle-Residences is a standalone source project (CAD files,
# full-res renders, brochure PDF) that isn't set up as its own git repo yet —
# unlike the other project folders here, which are submodules. Ignored so it
# can't get swept into a commit (e.g. via VS Code's "Smart Commit" auto-stage)
# before it has proper repo of its own.
CaletaResidences/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
